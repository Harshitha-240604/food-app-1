apiVersion: v1
kind: Service
metadata:
  name: {{ include "food-chart.fullname" . }}
  labels:
    {{- include "food-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "food-chart.selectorLabels" . | nindent 4 }}
